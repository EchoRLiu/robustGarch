// Implementation file.
#include "RobGARCH_header1.h"
#include<cmath>
#include<functional>
#include<algorithm>

// int main(vector<double> x){
  
  //int 
  
  
  //return 0;
//}

vector<double> bgarch11(vector<double> x){
  int n = x.size();
  //double v = varin(x);
  //vector<double> y = transform(x.begin(), x.end(), x.begin(), std::bind(std::divides<double>(), std::sqrt(v)));
  //y = y - std::median(y);
  
  return x;
}












